package retosFinalyPostwork.s5

// En nuestro proyecto

// reto final 5

/* Utilizacion de métodos de las estructuras de datos en el Menu del carrito
   aqui utilizamos la clase carrito para agregar los articulos de la base de
   datos del data class y poder guardarlos en el menu carrito como se muestre en el fragmento
   tanto como para modificarlos en un arraylist por el usuario

fragmento extraido del package controllers  archivo carritoControllers

    var nombre = Carrito()
    val productos: MutableMap<Int, Producto> = mutableMapOf()
    cargar(productos)

    nombre.agregarEsp(espacio= espaciosl)
    nombre.agregarEsp(espacio= espacios)
    nombre.agregarDisponibles(disponible = disponibles)
    nombre.agregarEsp(espacio= espaciosl)
    nombre.agregarSigno(signo = signo)
    nombre.agregarTotal(total=totalArticulo)
    listacarrito.add(nombre.imprimirNombre())          <---------------------
    nombre.clear()


*/